
services:
  db:
    image: mysql:8.0
    container_name: flaw_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Hemanth@170
      MYSQL_DATABASE: flaw_ai
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql



volumes:
  db_data:
